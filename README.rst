Deep Learning Repository
========================

First of all, be sure that the repo is in a virtual environment : source ../Script/activate

Then, to download the necessary installation run : make init

To run the test : make test
